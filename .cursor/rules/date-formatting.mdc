---
description: Date Formatting Standards for AI Coding Assistance
globs:
alwaysApply: true
---

# Date Formatting Standards

This rule ensures consistent date formatting across all code, documentation, and AI-generated content.

## CRITICAL: Always Use Current Date

**NEVER hardcode dates in code or documentation.** All dates must be 100% accurate and current. Always determine the current date dynamically from:

1. **System clock** (preferred for most cases)
2. **MCP services** (when available and appropriate)
3. **Environment variables** (for testing or specific scenarios)

**Key Principle**: Dates in code files must always reflect the actual current date, never outdated hardcoded values.

## Required Date Format

**All dates MUST be in ISO 8601 format: `YYYY-MM-DD`**

Examples:

- ✅ `2025-01-19` (correct)
- ❌ `2025-1-19` (missing leading zero)
- ❌ `19-01-2025` (wrong order)
- ❌ `January 19, 2025` (not ISO format)
- ❌ `2025/01/19` (wrong separator)

## Implementation Guidelines

### Python Code

```python
from datetime import datetime

# Get current date in YYYY-MM-DD format
current_date = datetime.now().strftime("%Y-%m-%d")

# For file headers, docstrings, etc.
def get_current_date() -> str:
    """Get current date in YYYY-MM-DD format."""
    return datetime.now().strftime("%Y-%m-%d")
```

### Documentation

```markdown
<!-- In markdown files -->

Date: 2025-01-19

<!-- Or use dynamic generation when possible -->

Last Updated: {{ current_date }}
```

### File Headers

```python
"""
Module description here.

Author: AI Assistant
Date: 2025-01-19  # Must be current date, not hardcoded
"""
```

## Common Use Cases

### 1. File Headers and Docstrings

```python
"""
Work Sessions Merger for WBSO Hours Registration

Author: AI Assistant
Date: 2025-01-19  # Always current date
"""
```

### 2. Logging and Timestamps

```python
import logging
from datetime import datetime

# Use ISO format for log timestamps
logging.basicConfig(
    format="%(asctime)s - %(levelname)s - %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)
```

### 3. Data Processing

```python
# When processing date-sensitive data
def process_data_with_date():
    current_date = datetime.now().strftime("%Y-%m-%d")
    # Use current_date in processing logic
```

### 4. Configuration Files

```python
# In configuration or metadata
config = {
    "generated_date": datetime.now().strftime("%Y-%m-%d"),
    "version": "1.0.0"
}
```

## AI Assistant Behavior

When AI assistants generate code or documentation:

1. **Always use current date** - never hardcode dates
2. **Use system clock** to determine current date
3. **Format as YYYY-MM-DD** consistently
4. **Update existing hardcoded dates** when modifying files
5. **Include date generation logic** in code when appropriate
6. **Ensure 100% accuracy** - dates must always reflect the actual current date

## Error Prevention

### Common Mistakes to Avoid

1. **Hardcoded dates**: `Date: 2025-01-15` ❌
2. **Wrong format**: `Date: 1/15/2025` ❌
3. **Missing leading zeros**: `Date: 2025-1-5` ❌
4. **Inconsistent separators**: `Date: 2025.01.15` ❌

### Validation

Before committing code:

- Check for hardcoded dates
- Verify date format is YYYY-MM-DD
- Ensure dates are current (not future or past dates unless intentional)

## Integration with Other Rules

This rule works with:

- [golden-rules.mdc](mdc:.cursor/rules/golden-rules.mdc) - Core development principles
- [coding-style.mdc](mdc:.cursor/rules/coding-style.mdc) - Code formatting standards
- [documentation-code-sync.mdc](mdc:.cursor/rules/documentation-code-sync.mdc) - Documentation standards

## Examples

### Good Examples

```python
# Dynamic date generation
current_date = datetime.now().strftime("%Y-%m-%d")

# File header with current date
"""
Script: data_processor.py
Author: AI Assistant
Date: 2025-01-19
"""

# Configuration with current date
config = {
    "created": datetime.now().strftime("%Y-%m-%d"),
    "version": "1.0.0"
}
```

### Bad Examples

```python
# Hardcoded date (WRONG)
"""
Script: data_processor.py
Author: AI Assistant
Date: 2025-01-15  # This will become outdated
"""

# Wrong format (WRONG)
current_date = "1/15/2025"  # Should be "2025-01-15"

# Missing leading zero (WRONG)
current_date = "2025-1-5"  # Should be "2025-01-05"
```

## Enforcement

This rule is enforced by:

- AI assistant behavior (always apply)
- Code review processes
- Pre-commit hooks (when applicable)
- Documentation standards

Remember: **Current date, current date, current date!** Never hardcode dates in any form of content generation. All dates must be 100% accurate and reflect the actual current date.
