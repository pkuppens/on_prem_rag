version: '3.8'
services:
  rag-backend:
    image: rag-backend:latest
    environment:
      - DATABASE_URL=${DATABASE_URL}
    ports:
      - '8000:8000'
    depends_on:
      - chroma
  chroma:
    image: chromadb/chroma:latest
    volumes:
      - chroma-data:/chroma
volumes:
  chroma-data:
